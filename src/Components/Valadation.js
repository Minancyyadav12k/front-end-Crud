import React from "react";
export default function Valadation(form) {
  let error={};
if (!form.email){
    error.email="Emai is requred"
}
    return error (
      <div>
        
      </div>
    )
  }

